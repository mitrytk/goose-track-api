const { HttpError, ctrlWrapper } = require("../helpers/index");
const { User } = require("../models/user");

//all controllers

// module.exports = {
//   getAll: ctrlWrapper(getAll), - как пример
// };
